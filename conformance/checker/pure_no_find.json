{
  "name": "pure_no_find",
  "category": "checker",
  "description": "Find is forbidden inside pure functions (reads shared runtime state)",
  "input": "pure no_find\n  do\n    bot = find \"room_42\"\n",
  "expected": {
    "outcome": "compile_error",
    "error_contains": ["find"],
    "error_kind": "error"
  }
}
