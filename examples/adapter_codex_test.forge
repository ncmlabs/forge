# Test: codex adapter error handling (expects usage limit error)

task codex_test
  gives Text
  do
    result = session "codex-test" agent "codex" prompt "say hello" timeout 30s gives AgentResult
    when result.sure -> give result.plan
    else -> give "ADAPTER_ERROR_HANDLED"

fn main
  say codex_test()
