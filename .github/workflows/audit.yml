name: Security Audit

on:
  schedule:
    - cron: "0 6 * * 1"
  push:
    paths: [Cargo.lock]

jobs:
  audit:
    name: Audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: rustsec/audit-check@v2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
